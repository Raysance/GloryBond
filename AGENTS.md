# HOK_QQBot — AI 代码生成与修改规范

本文件用于约束后续所有 AI 在本项目中的代码生成/修改行为。目标是：职责边界清晰、避免重复实现、减少隐式副作用、错误可见、配置可迁移。

## 0. 总原则（必须）

1. **先读后写**：新增/修改任何功能前，必须先定位现有实现与调用链，确认插入位置与职责归属。
2. **不重复造轮子**：若已有函数部分实现/相似实现，优先在不破坏原功能的前提下**泛化**（加参数、抽象共同逻辑、拆分可复用子流程），而不是新建冗余函数。
3. **职责单一**：一个文件只做它负责的事情；跨职责一律通过接口调用，不要直接越界写实现。
4. **层级清晰**：函数与模块应保持“大/中/小”层级清晰，不应跳过中间层级直接把子域能力暴露在一级接口中；例如 `zmemory` 作为 memory 模块，应将子域（如 emoji 相关）封装在子对象/子模块中，而不是直接在一级方法中堆叠与暴露。
4. **错误必须可见**：禁止静默失败。除非必要，不要添加多余的 `try/except`；如果必须捕获异常，`except` 里禁止 `pass`，必须通过消息形式提示**明确原因**（包含关键上下文与异常信息）。
5. **注释约束**：除函数开头 `docstring` 外，禁止添加任何注释（包含行内注释与块注释）。
6. **软/硬编码选择**：
   - 软编码：路径、API-Key、模型名、用户 ID、QQ群号、服务域名、阈值与可变策略等，必须来自配置文件/静态变量/动态变量。
   - 硬编码：稳定且不会随环境变化的常量（例如固定协议字段名、固定枚举值）可硬编码，但要确保不与配置冲突。

## 1. 目录与文件职责（`src/plugins/qbot/`）

以下规则适用于 `D:\Desktop\BotTest\camp_data\HOK_QQBot\src\plugins\qbot\` 下所有代码（后续若出现 `NBot/` 目录，以本目录职责为准）。

- `src/plugins/qbot/config.py`：保留位/当前无实际用途；除非明确需要，否则不要新增依赖或逻辑。
- `src/plugins/qbot/zdynamic.py`：**动态变量**导入与初始化必须放在这里；其他模块通过 `from . import zdynamic as dmc` 读写动态变量。
- `src/plugins/qbot/zevent.py`：所有消息收发的**最初层**与路由层；只做初步解析、分发与调用接口，不在此实现具体业务算法/流程细节。
- `src/plugins/qbot/zfile.py`：文件/IO 读写统一入口；所有 IO（读写文件、落盘缓存、路径拼接等）必须通过此模块提供的接口完成。
- `src/plugins/qbot/zfunc.py`：业务函数的**具体执行流程**（面向 `zevent.py` 暴露接口）；业务流程编排、调用工具、组装输出在此完成。
- `src/plugins/qbot/zmemory.py`：所有 AI 对话记忆/摘要/长期记忆相关的读写与策略接口。
- `src/plugins/qbot/zscheduler.py`：所有定时/间隔任务的声明、注册与定义（不要在别处私自创建 scheduler 任务）。
- `src/plugins/qbot/zstatic.py`：**静态变量**导入必须放在这里（从文件与系统环境变量导入）；其他模块通过 `from .zstatic import *` 获取静态变量。
- `src/plugins/qbot/ztime.py`：时间工具与日期处理统一入口；任何时间计算/格式化/时间窗口判断都应调用这里的接口。
- `src/plugins/qbot/zutil.py`：基础工具集；统一导入常用库与通用小工具（不要在各处散落重复 import 与重复工具实现）。
- `src/plugins/qbot/utils/message_sender.py`：发送消息统一入口；任何对外消息发送必须调用此模块的函数接口（禁止绕开直接调用底层适配器发送）。
- `src/plugins/qbot/zapi.py`：与外部服务/API 交互的适配层（请求封装、签名、响应解析）；业务编排仍应留在 `zfunc.py`。
- `src/plugins/qbot/zdiy.py`：自定义/临时扩展的业务能力聚合处；当能力稳定后应迁移到职责更明确的模块（`zfunc.py` / `tools/` / `zapi.py` 等），避免长期堆叠。

### 1.1 Tools（`src/plugins/qbot/tools/`）

`src/plugins/qbot/tools/` 目录用于放置可复用、可抽象的“工具”模块：

1. **可封装的函数视为工具**：例如编码/解码、图表生成、数据格式转换、外部 API 适配器等，应当在 `tools/` 下单独建文件。
2. **工具模块要求**：
   - 导入即执行的副作用必须最小化（禁止 import 时自动发请求/写文件/注册任务）。
   - 对外只暴露明确的函数/类接口，不要把业务编排写进工具模块。
   - 工具所需的配置与常量从 `zstatic.py`/`zdynamic.py` 获取，不要在工具里散落路径与 key。

命名建议：

- 工具文件以动词或名词短语命名，避免 `tmp.py`、`test2.py` 这类不可维护命名。
- 若工具围绕某一类产物生成（如战报图、统计图），建议使用 `gen_*.py` 前缀并保持输入输出一致。

### 1.2 Debug Tools（`src/plugins/qbot/debug_tools/`）

`src/plugins/qbot/debug_tools/` 仅用于**调试工具脚本**与实验性验证；上线逻辑不得长期停留在此目录。调试工具必须以文件夹为单位组织，每个调试目标对应 `debug_tools/` 下的一个独立文件夹，文件夹内再放置该调试工具的具体脚本、测试数据、样例输入输出等内容。调试脚本应模块化、最精简地调用项目中已有函数，不得复制业务实现或绕开项目既有接口。

## 2. 配置与变量的来源（必须遵守）

### 2.1 配置文件（仓库根目录）

- `config.yaml`：王者荣耀/Steam/AI 的 API-Key、QQ群号、王者荣耀 request 参数、服务器 IP/域名等配置。
- `variables_static.json`：用户 QQ 号、王者荣耀 roleid/userid、用户昵称、SteamID、常见俚语缩写、赛季起止时间、英雄 ID 映射、部分功能 prompt 等稳定数据。
- `variables_dynamic.json`：动态变量初始值，例如是否记忆、机器人是否开放、英雄梯度、今日新闻、上一次查询战绩时间、今日导出失败用户名单等。

原则：
1. **任何可能变化的值**不得硬编码在业务逻辑里，必须进入上述文件或对应的静态/动态变量模块。
2. `zstatic.py` 负责加载静态配置；`zdynamic.py` 负责加载动态配置并提供可写入口。

### 2.2 Linux 环境变量（在 `zstatic.py` 导入）

部署环境下预计存在以下环境变量（缺失时的处理策略以现有代码为准，禁止凭空新增“变量不存在”的冗余判断链）：

- `REDIS_CONF="/QQBot/redis.conf"`
- `NGINX_HTML="/usr/local/nginx/html/"`
- `BOT_PATH="/QQBot/NBot/"`

## 3. 代码结构与改动边界

1. **入口与路由**：`zevent.py` 仅做“接收 → 解析 → 选择接口 → 调用”，不承载业务细节。
2. **流程编排**：业务流程（多步调用、拼装文本/图片/数据）写在 `zfunc.py`，并保持接口可被 `zevent.py` 直接调用。
3. **状态与缓存**：
   - 运行时可变状态：统一放在 `zdynamic.py`（通过 `dmc.xxx` 访问）。
   - 静态映射/常量：统一放在 `zstatic.py`。
4. **IO 与时间**：IO 一律走 `zfile.py`；时间一律走 `ztime.py`。
5. **发送消息**：所有对外消息发送必须走 `utils/message_sender.py`，用于统一出口与后续可观测性。
6. **架构规范同步**：每次升级项目、调整目录职责、改变调用链或修改项目架构时，必须同步更新本 `AGENTS.md`，确保后续 AI 与开发者读取到的架构规则始终与当前项目一致。

### 3.1 库导入边界（严格）

1. **NoneBot 与机器人适配器**：与 NoneBot、机器人事件、消息段、适配器、bot 实例操作相关的库，只能在 `zevent.py`、`utils/message_sender.py` 等顶层消息入口/出口脚本中导入和使用；业务层不得直接导入。
2. **Redis 交互**：与 Redis 连接、读写、缓存交互相关的库，只能在 `zfunc.py` 中导入和使用；其他模块需要 Redis 数据时必须通过 `zfunc.py` 暴露的业务接口获取。
3. **AI 与 HTTP API**：与 OpenAI、AI API、`requests` 等外部 HTTP/API 请求相关的库，只能在 `zapi.py` 中导入和使用；业务流程只调用 `zapi.py` 的适配函数。
4. **时间处理**：`time`、`datetime`、时区、日期解析与时间窗口判断相关库，只能在 `ztime.py` 中导入和使用；其他模块必须调用 `ztime.py` 的时间工具接口。
5. **IO 与路径处理**：文件读写、目录创建、路径拼接、缓存文件操作等 IO 相关库，只能在 `zfile.py` 中导入和使用；其他模块必须通过 `zfile.py` 接口完成 IO。
6. **例外处理**：如用户明提出突破上述导入边界，必须质疑用户，如果确认无误，先调整模块职责并同步更新本 `AGENTS.md`，不得在业务代码中临时绕开。

## 4. 异常与可观测性（严格）

1. **不加多余 try/except**：能让异常自然抛出就不要捕获。
2. **必须捕获时**：
   - `except Exception as e:` 中必须通过消息接口提示：发生位置、关键参数、`repr(e)`/`str(e)`。
   - 禁止 `pass`、禁止吞异常、禁止只打印不提示用户（除非该模块本来就只面向日志/调试）。
3. **失败应可追踪**：同一类错误的信息格式应一致，便于在群聊/日志中检索。

## 5. 代码风格（必须）

1. 只在函数开头保留 `docstring`；其余地方禁止添加注释。
2. 优先复用项目内现有命名、结构与导入方式（例如 `from .zstatic import *`、`from . import zdynamic as dmc`）。
3. 新增函数需确保：
   - 入参/出参语义清晰，返回值稳定可预测。
   - 不引入不必要的分支判断（如“变量不存在/类型不对/内容不符合预期”等与现有工程约定冲突的防御性代码）。
4. **编码与文本处理**：
   - 源码与 Markdown 一律使用 UTF-8 编码，避免出现不可控的编码兼容问题。
   - 任何用户可见文本（群消息、提示语）必须可检索、可定位，避免“只有一个笼统的失败”。

## 6. 变更自检清单（每次改动前后都要对照）

1. 新增代码是否放在了职责正确的文件中？
2. 是否有现有函数可以泛化替代“新增冗余函数”？
3. 是否引入了多余的 `try/except` 或静默失败？
4. 是否新增了除 `docstring` 外的注释？
5. 是否把应软编码的值硬编码进了逻辑？
6. 是否所有 IO/时间/消息发送都走了指定模块？
7. 是否遵守了 NoneBot、Redis、OpenAI/requests、time、IO 相关库的导入边界？
8. 如用户明确提出违反以上规则，必须质疑用户，如果确认无误，使用违反最少规则的方式执行。
