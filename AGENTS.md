# HOK_QQBot_sc Agent Notes

本文件适用于项目根目录及其所有子目录。更深层目录中的 `AGENTS.md` 可补充或覆盖对应目录的细则。

## 0. 总原则（必须）

1. **先读后写**：新增/修改任何功能前，必须先定位现有实现与调用链，确认插入位置与职责归属。
2. **不重复造轮子**：若已有函数部分实现或相似实现，优先在不破坏原功能的前提下泛化，而不是新建冗余函数。
3. **职责单一**：一个文件只做它负责的事情；跨职责一律通过接口调用，不要直接越界写实现。
4. **层级清晰**：函数与模块应保持“大/中/小”层级清晰，不应跳过中间层级直接把子域能力暴露在一级接口中。
5. **错误必须可见**：禁止静默失败；如必须捕获异常，`except` 中禁止 `pass`，必须暴露明确原因与关键上下文。
6. **注释约束**：除函数开头 `docstring` 外，禁止添加无必要注释；能通过命名表达的，不额外写解释性注释。
7. **软/硬编码选择**：路径、API-Key、模型名、用户 ID、QQ群号、服务域名、阈值与可变策略等必须来自配置文件、静态变量、动态变量或环境变量；稳定协议字段和固定枚举值可硬编码。
8. **文档同步**：发生用户可见功能、使用方式、演示入口或公开说明变更时，必须同步更新 `README.md`；实现细节、仓库维护规则、部署细节与目录职责应写入对应 `AGENTS.md` 或开发文档，不写入 `README.md`。

## 1. 仓库边界

- 根目录仓库用于提交到 public repo 做展示，必须避免纳入密钥、真实配置、用户数据、运行日志、私有缓存和部署凭据。
- `NBot/` 与 `Web/` 是可单独提交到 private repo 的子项目，子目录内的 `.gitignore` 只忽略运行缓存、编译产物、临时文件等不必要文件。
- 敏感文件的忽略规则统一放在根目录 `.gitignore`，避免 public repo 意外展示真实环境信息。
- 根目录 `README.md` 只用于功能介绍、使用入口和公开展示，不写实现细节、仓库维护规则、部署细节、目录职责、真实 QQ 号、群号、API-Key、服务器凭据、私有域名管理信息或可反推出用户隐私的数据。

## 2. 目录职责

- `NBot/`：QQ 机器人与王者荣耀业务核心，具体代码规范以 `NBot/AGENTS.md` 及更深层规范为准。
- `Web/`：数据展示、管理后台与相关页面服务，具体代码规范以 `Web/AGENTS.md` 为准。
- `assets/`：公开 README 展示资源，只放可公开的截图、示例图和演示素材。
- `.github/`：根仓库展示或自动化工作流配置；涉及私有部署的配置应保留在 private repo 或使用 GitHub Secrets。

## 3. Gitignore 分层规则

- 根目录 `.gitignore` 负责 public repo 安全边界，优先忽略敏感配置、密钥、真实数据、日志和部署凭据。
- 子目录 `.gitignore` 负责子项目开发体验，只忽略 `__pycache__/`、`.venv/`、构建输出、临时目录、生成文件等不需要备份的文件。
- 不要在子目录 `.gitignore` 中新增密钥类规则；发现新的敏感文件类型时，应更新根目录 `.gitignore` 并同步记录到本文件或对应开发文档。
- 不要在根目录 `.gitignore` 中忽略公开展示必需的源码、模板、示例文档或 README 图片素材。

## 4. 变更自检清单

1. 新增代码是否放在职责正确的目录和文件中？
2. 是否复用了现有实现，避免重复函数或重复路由？
3. 是否避免了静默失败、无意义兜底和多余 `try/except`？
4. 是否把可变配置、密钥、路径和用户数据从代码中剥离？
5. 是否遵守根仓库 public、子项目 private 的提交边界？
6. 用户可见功能说明是否同步到 `README.md`，实现与维护规则是否同步到必要的 `AGENTS.md`？
